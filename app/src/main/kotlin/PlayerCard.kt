import android.media.Image

class PlayerCard(
    val isBasic: Boolean,
    val image: Image?,
    val name: String,
) {
    val  hight = 100
    fun whenUsed(){
        Player.all
        player.activ
        player[val pers: Personages]
            .giveCard(int)
            .giveMoney(int)
            .giveMjolnir(int)
            .giveHP(int)
    }
    fun when
    val cost = 0
    val basicCard = mapOf<Personages, Card>()

}